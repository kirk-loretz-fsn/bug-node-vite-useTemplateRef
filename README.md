# Setup
```shell
pnpm install
```

## Works
```shell
pnpm run build
```
```shell
node dist/ssr.js
```

## Fails
```shell
pnpm run ssr
```

## Also
`ref()` works while `useTemplateRef()` causes an error.
